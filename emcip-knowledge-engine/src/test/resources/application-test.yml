spring:
  kafka:
    consumer:
      auto-offset-reset: earliest

knowledge:
  embedding:
    dimension: 1024
  llm-orchestrator:
    base-url: http://localhost:${mockwebserver.port:9999}

logging:
  level:
    io.emcip: DEBUG
